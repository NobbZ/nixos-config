name: "Test"
on:
  pull_request:
  push:
jobs:
  builds:
    name: Build configuration for ${{ matrix.host }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        host:
        - nixos
        - tux-nixos
    steps:
    - uses: actions/checkout@v2.3.1
    - uses: cachix/install-nix-action@v11
      with:
        install_url: https://github.com/numtide/nix-flakes-installer/releases/download/nix-3.0pre20201020_e0ca98c/install
        extra_nix_config: |
          experimental-features = nix-command flakes
    - uses: cachix/cachix-action@v6
      with:
        name: mycache
        signingKey: '${{ secrets.CACHIX_SIGNING_KEY }}'
    - run: nixos-rebuild build --flake .#${{ matrix.host }} -L
