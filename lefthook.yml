pre-commit:
  parallel: true
  commands:
    formatting:
      glob: "*.nix"
      run: nixpkgs-fmt --check {all_files}

    instantiate:
      run: nix-instantiate --eval ./overlays.nix

    # linting:
    #   glob: "*.nix"
    #   exclude: nix/sources.nix
    #   run: nix-linter {all_files}
